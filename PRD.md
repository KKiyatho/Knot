# 매듭 (Knot) 제품 요구사항 정의서

- 문서 상태: Draft for MVP
- 버전: 1.0
- 최종 갱신: 2026-09-05
- 대상 플랫폼: Flutter 모바일 앱 우선, 웹 뷰어/랜딩 페이지 후속

## 1. 제품 정의

### 1.1 한 문장 설명
매듭은 사용자가 루틴을 완료했다는 사실을 텍스트나 사진 없이 한 번의 길게 누르기로 남기고, 같은 시간대에 같은 행동을 한 익명의 사람들과 감각적으로 연결되는 미니멀 루틴 앱이다.

### 1.2 문제
기존 루틴 앱은 기록 입력, 인증 사진, 통계, 경쟁과 같은 지속적인 관리 부담을 만든다. 반대로 완전히 혼자 하는 루틴은 동기와 연결감을 제공하지 못한다.

### 1.3 해결책
- 루틴 완료를 2초 길게 누르기 하나로 축소한다.
- 개인 프로필, 댓글, 좋아요, 팔로우 없이 익명 이벤트만 공유한다.
- 실시간 사용자가 부족해도 최근 이벤트를 후보로 활용해 연결감이 끊기지 않게 한다.
- 점, 선, 진동, 짧은 소리만으로 타인의 존재를 표현한다.

### 1.4 제품 원칙
1. 기록보다 행위: 사용자가 입력해야 할 내용을 최소화한다.
2. 연결하되 노출하지 않기: 개인 식별 정보와 사회적 평가를 저장하거나 표시하지 않는다.
3. 실시간성은 정직하게 연출하기: 과거 이벤트를 현재 이벤트처럼 데이터상 위장하지 않고, 클라이언트에서 재생 시점을 분리한다.
4. 여백을 기능으로 사용하기: 정보량보다 집중과 회복을 우선한다.
5. 실패는 조용하게: 네트워크나 오디오 권한이 없어도 핵심 루틴 인증은 중단하지 않는다.

## 2. 사용자와 사용 상황

### 2.1 핵심 사용자
- 일기와 루틴 앱의 입력 피로를 느끼는 사람
- 목표 달성에 타인의 존재감은 필요하지만 소셜 네트워크는 부담스러운 사람
- 아침 기상, 물 마시기, 잠들기처럼 반복 행동을 조용히 습관화하려는 사람

### 2.2 대표 사용 시나리오
1. 사용자가 앱을 열고 오늘의 루틴 화면을 본다.
2. 중앙의 얇은 선을 2초간 누른다.
3. 진행 중 진동이 점진적으로 강해지고, 손을 떼면 진행률이 초기화된다.
4. 2초를 채우면 내 매듭이 선 위에 생성되고 짧은 완료 효과음이 난다.
5. 다른 익명 사용자의 매듭이 1~2초 간격으로 순차적으로 나타난다.
6. 약 5초 후 선과 점이 자연스럽게 사라지고 다음 루틴 대기 상태로 돌아간다.

### 2.3 제외 범위
- 공개 프로필, 친구, 팔로우, 댓글, 좋아요, 순위표
- 사진/영상 인증과 자유 텍스트 입력
- 개인별 상세 분석 대시보드와 경쟁형 streak
- MVP에서 사용자 맞춤 루틴 생성
- 사용자 간 직접 메시지

## 3. MVP 범위

### 3.1 포함 기능
- 익명 세션 시작
- 기본 루틴 1~3개: 기상, 물 마시기, 잠들기
- 오늘의 루틴 선택 또는 순차 표시
- 2초 롱프레스 인증
- 진행률 시각화와 지원 햅틱
- 내 매듭과 타인의 매듭 애니메이션
- 3-Tier 매칭 후보 수집
- 매칭 결과의 순차 재생
- 매칭 후 약 5초 뒤 초기화
- 오프라인/실패 상태의 조용한 복구
- Firebase 보안 규칙과 익명 식별자 기반 데이터 접근
- 기본 이벤트 모니터링: 인증 성공률, 매칭 지연, 오류율

### 3.2 MVP 성공 기준
출시 후보 빌드에서 다음을 충족한다.

| 지표 | 목표 |
|---|---:|
| 루틴 인증 성공률 | 정상 네트워크에서 99% 이상 |
| 롱프레스 완료 시간 | 2.0초 ± 100ms |
| Tier 1 후보 수신 | 활성 세션의 95%가 2초 이내 |
| 매칭 결과 표시 | 인증 후 3초 이내에 최소 1개 |
| 매칭 공백률 | 정상 네트워크에서 1% 미만 |
| 타인 이벤트의 개인정보 노출 | 0건 |
| 애니메이션 프레임 드롭 | 지원 기기에서 체감 가능한 끊김 없음 |

여기서 “100% 매칭”은 항상 실제 동시 접속자가 존재한다는 뜻이 아니라, 후보가 없을 때 Tier 2와 Tier 3로 대체해 사용자에게 빈 화면을 남기지 않는 제품 경험 목표다.

## 4. 상세 기능 요구사항

### 4.1 루틴 상태
루틴은 다음 상태를 가진다.

- `ready`: 인증 대기
- `holding`: 사용자가 누르는 중
- `completed`: 내 이벤트가 서버에 기록됨
- `replaying`: 매칭 후보를 순차 재생 중
- `cooldown`: 화면 초기화 애니메이션 중
- `offline`: 네트워크가 끊겼지만 재시도 가능
- `error`: 사용자 동작을 방해하지 않는 오류 안내 상태

한 루틴은 중복 인증을 기본적으로 허용하지 않는다. 같은 루틴을 다시 인증하려면 다음 UTC 날짜 또는 명시된 재시도 정책이 필요하다. 서버는 클라이언트 상태와 무관하게 중복 이벤트를 멱등적으로 거부해야 한다.

### 4.2 롱프레스 인증
- 인증 임계 시간은 2,000ms다.
- 터치 시작부터 진행률을 0에서 1까지 선형 또는 완만한 ease-out으로 표시한다.
- 2초 전에 손가락을 떼거나 포인터가 유효 영역을 벗어나면 진행률을 0으로 되돌린다.
- 완료 순간에 검은 원형 점을 선 위에 생성한다.
- 완료 효과음은 짧은 단발음이어야 하며 시스템 볼륨 정책을 따른다.
- 햅틱은 디바이스가 지원할 때만 사용한다. 미지원/권한 거부 시 인증은 정상 동작해야 한다.
- 접근성을 위해 최소 터치 영역과 보조 라벨을 제공한다. 햅틱과 색상만으로 상태를 전달하지 않는다.

### 4.3 하이브리드 매칭
서버는 이벤트 인증 직후 다음 우선순위로 후보를 수집한다.

1. Tier 1: 인증 시각 기준 ±2초 안에 같은 루틴을 완료한 다른 세션
2. Tier 2: 최근 1분 안에 같은 루틴을 완료한 다른 세션
3. Tier 3: 최근 1시간 안에 저장된 익명 매듭 캐시

후보 규칙:
- 현재 세션의 이벤트는 후보에서 제외한다.
- 동일한 `eventId`는 한 번만 반환한다.
- 반환 순서는 최신 이벤트 우선이며, 각 Tier의 우선순위는 시간순보다 높다.
- 후보 수가 설정된 목표 수보다 적으면 다음 Tier를 추가한다.
- MVP 목표 후보 수는 사용자 이벤트 포함 최대 8개이며, 최소 1개의 타인 후보를 목표로 한다.
- 캐시 후보는 실제 발생 시각을 내부적으로 보존하되 화면에는 “몇 분 전” 같은 상대 시각을 표시하지 않는다.
- 클라이언트는 후보의 Tier와 발생 시각을 받아 재생 지연을 결정한다. 서버가 과거 이벤트의 발생 시각을 현재 시각으로 변경하지 않는다.

권장 재생 방식:
- 내 매듭: 즉시 표시
- Tier 1: 수신 즉시 또는 200~800ms 지연
- Tier 2: 1~2초 간격으로 표시
- Tier 3: 1~2초 간격으로 표시
- 전체 재생 시간은 기본 5초, 후보 수에 따라 최대 10초로 제한

### 4.4 시청각 피드백과 초기화
- 배경은 흰색, 기본 선과 점은 검정 또는 다크그레이로 유지한다.
- 타인의 점은 작은 fade-in과 scale-in으로 생성한다.
- 타인 점 하나가 나타날 때 짧은 crackle/wood-tap 계열 효과음을 한 번 재생한다.
- 오디오는 사용자 설정, 브라우저 자동재생 제한, 운영체제 무음 모드를 존중한다.
- 마지막 점이 생성된 뒤 기본 5초 후 선과 점이 fade-out 또는 화면 밖 이동으로 사라진다.
- 초기화 중 새 인증이 들어오면 현재 재생을 취소하고 새 이벤트를 우선한다.

### 4.5 익명성과 세션
- 가입/로그인 없이 익명 인증으로 세션을 시작한다.
- 클라이언트에는 무작위 `sessionId`만 제공한다.
- 이메일, 전화번호, 연락처, 정밀 위치, 광고 식별자를 MVP 데이터 모델에 저장하지 않는다.
- 세션 식별자는 일정 기간 후 폐기하거나 회전할 수 있어야 한다.
- 운영 로그에는 원문 식별자 대신 해시 또는 집계값을 사용한다.

## 5. 정보 구조와 화면

### 5.1 MVP 화면
1. 온보딩/동의: 익명성, 소리와 햅틱 사용, 데이터 보존 기간을 짧게 설명
2. 루틴 홈: 오늘의 루틴명, 중앙 선, 현재 상태, 접근성 라벨
3. 매칭 재생: 선 위의 내 점과 타인 점, 최소한의 상태 표현
4. 설정: 소리 켜기/끄기, 햅틱 켜기/끄기, 데이터/개인정보 안내
5. 오류/오프라인 상태: 재시도와 현재 동작 가능 여부만 표시

### 5.2 시각 원칙
- 스위스 타이포그래피 기반의 강한 정렬과 넓은 여백
- 장식 아이콘과 카드 남용 금지
- 시각 계층은 루틴명, 중앙 선, 점의 순서로 제한
- 검정/흰색 중심을 유지하되 상태 오류에 필요한 색상은 접근성을 위해 최소 사용
- 모션은 짧고 예측 가능해야 하며 `reduce motion` 설정을 존중

## 6. 기술 설계

### 6.1 권장 스택
- 앱: Flutter, Dart
- 인증: Firebase Anonymous Authentication
- 실시간 이벤트: Firebase Realtime Database
- 서버 검증/큐 조정: Cloud Functions for Firebase 또는 동등한 서버 런타임
- 배포/웹 뷰어: Azure Static Web Apps
- 분석: 개인정보를 포함하지 않는 집계형 이벤트 로그

### 6.2 논리 데이터 모델
Realtime Database 예시:

```text
/routines/{routineId}
  name: string
  enabled: boolean
  sortOrder: number

/events/{routineId}/{bucket}/{eventId}
  sessionHash: string
  createdAt: serverTimestamp
  expiresAt: serverTimestamp
  tierEligibleUntil: serverTimestamp

/activeSessions/{routineId}/{eventId}
  sessionHash: string
  createdAt: serverTimestamp
  expiresAt: serverTimestamp

/users/{sessionHash}/claims/{routineId}/{dateKey}
  eventId: string
  createdAt: serverTimestamp
```

`bucket`은 시간 단위 파티션 또는 TTL 정리 단위다. 클라이언트는 보안 규칙상 타인의 원시 세션 식별자와 사용자 경로를 직접 읽지 않고, 매칭 함수가 반환하는 최소 이벤트만 읽는다.

### 6.3 매칭 API 계약
논리적 요청:

```json
{
  "routineId": "water",
  "eventId": "client-generated-idempotency-key",
  "occurredAt": "server-assigned-time"
}
```

논리적 응답:

```json
{
  "matchId": "opaque-id",
  "routineId": "water",
  "replay": [
    {
      "eventId": "opaque-event-id",
      "tier": 1,
      "occurredAt": "server-time",
      "replayDelayMs": 400
    }
  ],
  "expiresAt": "server-time"
}
```

클라이언트가 보낸 시간은 정렬 힌트일 뿐 신뢰하지 않는다. 서버 시간이 권위 있는 시간이다.

### 6.4 보안과 남용 방지
- Database Rules로 세션이 자신의 claim만 생성할 수 있게 한다.
- 이벤트 생성은 Cloud Function 등 서버 검증 경로를 통과시킨다.
- 날짜/루틴별 중복 인증을 트랜잭션으로 방지한다.
- 세션별 rate limit과 IP/디바이스 지문 기반의 최소 남용 방지를 적용하되, 지문 원문은 저장하지 않는다.
- 이벤트 TTL을 1시간으로 제한하고 운영 로그와 분석 데이터의 보존 기간도 별도로 정의한다.
- Firebase Security Rules 테스트를 CI에서 실행한다.

## 7. 오류 및 엣지 케이스

- 네트워크가 끊긴 채 롱프레스를 완료하면 로컬에 미확정 상태를 표시하고 서버 재연결 후 한 번만 제출한다.
- 서버 제출이 타임아웃이면 자동 재시도하되 같은 `eventId`를 사용한다.
- 이미 제출된 이벤트가 재전송되면 기존 결과를 반환한다.
- 후보가 실제로 0개면 빈 화면 대신 내 매듭의 안정적인 완료 상태를 보여주고 다음 루틴으로 복귀한다. 테스트 환경에서는 합성 데이터를 사용하지만 운영에서는 합성 사용자로 가장하지 않는다.
- 오디오 재생 실패는 오류로 간주하지 않는다.
- 앱이 백그라운드로 전환되면 롱프레스는 취소하고 재생 타이머를 정리한다.
- 기기 시간이 틀려도 서버 시간이 매칭과 TTL을 결정한다.

## 8. 비기능 요구사항

- 성능: 홈 화면 첫 인터랙션 가능 시점 2초 이내를 목표로 한다.
- 안정성: 매칭 요청은 멱등적이며 재시도 가능해야 한다.
- 접근성: VoiceOver/TalkBack 라벨, reduce motion, 햅틱/오디오 대체 상태를 지원한다.
- 개인정보: 최소 수집, 짧은 보존, 삭제 가능한 데이터 구조를 유지한다.
- 관측성: 인증 성공/실패, API 지연, Tier별 후보 비율, 초기화 실패를 개인정보 없이 집계한다.
- 국제화: 루틴명과 설정 문구는 향후 다국어를 고려해 리소스 파일로 관리한다.

## 9. 테스트 요구사항

### 9.1 단위 테스트
- 2초 경계값, 조기 해제, 재진입 처리
- Tier 1/2/3 후보 분류와 중복 제거
- replay delay 계산과 최대 재생 시간 제한
- 멱등 키 재전송
- TTL 만료와 서버 시간 기준 처리

### 9.2 통합 테스트
- 익명 세션 생성부터 이벤트 제출, 매칭 응답까지
- 동시에 여러 사용자가 같은 루틴을 완료하는 경우
- 후보 부족 시 각 Tier로 fallback하는 경우
- Firebase Rules에서 허용/거부되어야 하는 읽기와 쓰기
- 오프라인 후 재연결과 중복 제출

### 9.3 수동/디바이스 테스트
- iOS/Android 대표 기기에서 햅틱과 오디오
- 무음 모드, 오디오 권한 거부, reduce motion
- 느린 네트워크와 앱 백그라운드 전환
- 작은 화면과 큰 화면에서 선/점/문자 겹침 여부

## 10. 출시 단계

### Phase 0: 기반
- Flutter 앱 셸, Firebase 프로젝트 분리, 환경 변수와 CI
- 익명 인증, 기본 루틴 seed, Rules 테스트

### Phase 1: 핵심 루프
- 롱프레스, 햅틱/오디오 추상화, 점 애니메이션
- 이벤트 멱등성, Tier 1 실시간 매칭

### Phase 2: fallback과 운영
- Tier 2/3 큐, TTL 정리, rate limit
- 오류 복구, 집계형 모니터링, 베타 플래그

### Phase 3: 베타 검증
- 실제 동시성 부하 테스트
- 매칭 지연과 빈 화면률 확인
- 기본 루틴 1~3개로 제한 출시

### 이후 로드맵
- 사용자 맞춤 루틴 이름
- 웨어러블 컴플리케이션
- 지역/시간대 기반 매칭 실험은 개인정보와 공정성 검토 후 별도 결정

## 11. 미결정 사항

- 하루에 같은 루틴을 몇 회까지 인증할 것인가
- MVP에서 루틴을 사용자가 선택하는지, 시간대에 따라 자동 제안하는지
- 매칭 후보의 목표 개수를 3개, 5개, 8개 중 무엇으로 고정할지
- 소리 파일의 라이선스와 원본 제작 방식
- Firebase 리전, 예상 DAU, 비용 상한
- 계정 삭제 요청과 익명 세션 폐기 UX

이 항목들은 구현 전에 결정하되, 기본값이 필요한 경우 Agent.md의 보수적인 기본 정책을 따른다.
