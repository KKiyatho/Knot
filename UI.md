# Knot UI 지침

## 1. UI의 한 문장

Knot은 루틴 앱이 아니라, 일주일 동안 35개의 점을 이어 하나의 형상을 완성하는 조용한 주간 매듭이다.

사용자는 매일 루틴을 완료할 때마다 도형 위의 빈 점 하나를 채운다. 점이 채워지면 인접한 점과 선으로 연결되고, 7일이 끝나면 물고기, 집, 잎, 달, 배, 산 등 하나의 형상이 완성된다.

## 2. 절대 원칙

- 화면에는 현재 주간 도형의 틀, 점, 선, 오늘의 루틴 상태만 둔다.
- 기본 화면에 카드, 배지, 통계, 순위, 소셜 지표, 장식 일러스트를 추가하지 않는다.
- 사용자가 해야 할 주 행동은 점을 길게 누르는 것 하나다.
- 메뉴와 설정은 하단의 작은 메뉴 영역에서만 연다.
- 색상, 햅틱, 소리 중 하나가 없어도 상태와 행동을 이해할 수 있어야 한다.
- 화면에 보이는 정보보다 도형의 변화를 우선한다.

## 3. 기본 화면

### 3.1 구성

기본 화면은 다음 네 요소로만 구성한다.

1. 상단: 현재 요일과 루틴 진행 상태를 짧게 표시한다.
2. 중앙: 선택한 주간 도형의 완성 틀을 표시한다.
3. 현재 점: 오늘 선택된 루틴에 해당하는 점을 표시한다.
4. 하단: 루틴 메뉴와 최소 설정 진입점을 둔다.

도형은 화면 중앙에 크게 배치하되, 화면 가장자리와 충분한 여백을 둔다. 도형 뒤에는 설명용 패널이나 카드가 없어야 한다.

### 3.2 도형 틀

- 빈 틀은 낮은 대비의 따뜻한 회색 또는 흐린 흑색 선으로 표시한다.
- 틀은 최종 형상의 연결 경로와 35개 노드 위치를 모두 포함한다.
- 완성된 점과 선은 짙은 흑색으로 표시한다.
- 빈 점, 완료 점, 현재 점은 모양과 명도 차이로 구분한다.
- 도형은 장식용 배경이 아니라 사용자의 주간 진행을 담는 작업 공간이다.
- 도형마다 점 위치와 연결 순서를 사전에 정의한다. 임의의 선을 실시간으로 그리지 않는다.

### 3.3 35개 노드 규칙

- 한 주는 7일, 하루는 5개 루틴으로 고정한다.
- 노드는 `dayIndex * 5 + routineIndex`로 식별한다.
- 하루의 5개 루틴은 같은 날의 작은 구간을 완성한다.
- 완료된 노드는 인접한 완료 노드와 연결된다.
- 아직 완료되지 않은 노드의 틀과 예정 연결선은 계속 보인다.
- 루틴을 건너뛰거나 실패하면 해당 노드는 비어 있는 상태로 남는다. 운영 데이터에 가짜 완료 이벤트를 만들지 않는다.
- 주간 종료 시 완성률을 별도 숫자로 강조하지 않는다. 완성된 도형 자체가 결과다.

## 4. 주간 시작과 도형 선택

### 4.1 선택 원칙

도형 선택은 주간 시작 시 1회 제공한다. 도형을 먼저 선택하고 그 도형에 35개의 루틴 노드를 배치한다.

선택 화면에는 카드 목록이나 긴 설명 대신, 작은 도형 미리보기와 형상 이름만 표시한다. 선택 가능한 형상은 초기에는 3~5개로 제한한다.

초기 형상 예시:

- 물고기: 몸통과 꼬리가 35개 점으로 이어지는 흐름형
- 집: 지붕, 벽, 문, 창이 완성되는 안정형
- 잎: 줄기와 잎맥이 자라는 자연형
- 달: 초승달 윤곽과 내부 점이 채워지는 야간형
- 산: 능선과 계곡이 연결되는 풍경형

형상은 단순히 SVG 장식으로 제공하지 않는다. 각 형상은 다음 메타데이터를 가진다.

- 35개 노드 좌표
- 노드 순서
- 인접 연결 목록
- 하루 단위로 보이는 진행 구간
- 접근성용 형상 이름과 완료 설명

### 4.2 도형 변경

주간 진행 중 도형을 변경하면 이미 쌓인 기록의 의미가 흔들리므로 기본적으로 허용하지 않는다. 새 도형은 다음 주에 선택한다.

사용자가 실수로 선택한 경우에 한해 주간 시작 직후, 아무 노드도 완료되지 않은 동안 1회 변경할 수 있다.

## 5. 루틴 메뉴

### 5.1 기본 상태

하단 drawer에는 오늘의 다섯 루틴을 작고 명확한 목록으로 표시한다. 각 행은 낮은 높이로 유지하고, 상세 반복 설정이나 설명은 열림 상태에서도 숨긴다. 사용자는 현재 루틴과 다음 행동을 한눈에 알아야 한다.

### 5.2 열림 상태

하단 메뉴를 누르면 오늘의 5개 루틴을 세로 목록으로 연다.

- 각 행에는 루틴명, 완료 여부, 인증 가능한 상태만 둔다.
- 루틴 추가, 자유 텍스트 입력, 복잡한 반복 설정은 MVP에서 제공하지 않는다.
- 기본 루틴은 기상, 물 마시기, 식사, 움직이기, 잠들기로 한다.
- 완료한 루틴은 다시 인증할 수 없다.
- 목록에서 루틴을 선택하면 중앙 도형의 현재 노드가 해당 위치로 이동하거나 강조된다.

### 5.3 인증

인증 가능한 루틴을 선택하면 사용자는 중앙의 현재 노드를 2초 동안 누른다.

- 누르는 동안 현재 노드의 진행만 표시한다.
- 2초 전에 손을 떼면 노드는 빈 상태로 돌아간다.
- 2초가 끝나면 점을 채우고, 가능한 경우 인접 완료 점과 선을 연결한다.
- 인증이 끝난 뒤 메뉴를 자동으로 닫고 도형에 시선을 돌려준다.
- 인증 중에는 다른 메뉴와 루틴 선택을 막는다.

## 6. 색상과 재료감

### 6.1 기본 팔레트

따뜻한 종이색과 흑색을 중심으로 하되, 화면 전체가 한 가지 색조로 뭉개지지 않게 사용한다.

- 배경: `#F4EFE6` 또는 기기 밝기에 맞춘 따뜻한 아이보리
- 주 선과 완료 점: `#1D1C1A`
- 빈 틀: `#C9C0B4`
- 보조 텍스트: `#6E675E`
- 현재 상태: `#B86A45` 계열의 낮은 채도 테라코타
- 오류/연결 끊김: 접근 가능한 적색 계열을 제한적으로 사용

색상만으로 완료 여부를 전달하지 않는다. 점의 채움, 윤곽, 라벨과 함께 사용한다.

### 6.2 스타일

- 모던하고 절제된 산세리프 글꼴을 사용한다.
- 제목은 짧고 작게 둔다. 큰 영웅 문구나 마케팅 카피를 넣지 않는다.
- 모서리가 둥근 카드, 유리 효과, 그림자, 그라데이션, 장식용 원형 배경을 사용하지 않는다.
- 선은 얇고 일정한 굵기를 유지한다.
- 도형의 모양과 점이 화면의 시각적 중심이어야 한다.

## 7. 모션과 사운드

- 주간 도형은 화면 진입 때 천천히 나타나고, 레이아웃을 밀어내지 않는다.
- 점은 채워질 때 짧게 나타나며, 연결선은 점 이후에 그려진다.
- 하루의 다섯 점이 모두 완료되면 작은 호흡 모션만 허용한다.
- 주간 완성 시 전체 도형을 과장되게 폭발시키지 않는다. 완성된 선을 잠시 유지한 뒤 다음 주로 이동한다.
- 매칭 후보의 점은 도형의 별도 영역에서 조용히 나타내며 현재 사용자의 노드와 혼동되지 않아야 한다.
- `reduce motion`에서는 모든 이동을 즉시 상태 변경으로 대체한다.
- 사운드와 햅틱은 선택 기능이며, 실패해도 점과 선의 시각적 변화는 유지한다.

## 8. 메뉴와 설정의 경계

하단 메뉴에서 다음 설정만 제공한다.

- 소리 켜기/끄기
- 햅틱 켜기/끄기
- 현재 주간 도형 보기
- 개인정보와 데이터 보존 안내
- 데이터 초기화 요청

설정 화면은 도형을 가리지 않는 별도 화면 또는 바텀 시트로 열며, 기본 화면에 설정 항목을 상시 노출하지 않는다.

## 9. 상태별 화면 계약

| 상태 | 중앙 도형 | 하단 메뉴 | 사용자에게 보이는 정보 |
|---|---|---|---|
| 준비 | 빈 틀과 오늘의 빈 노드 | 오늘의 루틴 | 현재 루틴명 |
| 누르는 중 | 현재 노드 진행 표시 | 잠김 | 진행 상태 |
| 인증 완료 | 점 채움과 연결선 | 다음 루틴 | 완료 상태 |
| 매칭 재생 | 내 점과 익명 후보 점 | 잠시 숨김 | 최소 상태 라벨 |
| 오프라인 | 틀과 로컬 미확정 점 | 재시도 가능 | 재연결 상태 |
| 주간 완성 | 완성된 도형 | 다음 주 준비 | 완성 안내 |

## 10. 접근성 및 반응형 기준

- 중앙 인증 영역은 최소 48dp 이상 터치 가능 영역을 확보한다.
- 도형만 보고 현재 루틴을 알 수 없으므로 접근성 라벨로 루틴명, 요일, 완료 여부를 읽어준다.
- 큰 글자 설정에서 루틴명과 도형이 겹치지 않도록 도형 영역을 축소한다.
- VoiceOver/TalkBack은 35개 점 전체를 무의미하게 순회하지 않도록 오늘의 노드와 주간 요약을 우선 제공한다.
- 색각 이상, 무음, 햅틱 미지원 환경에서도 완료 여부를 이해할 수 있어야 한다.
- 작은 화면에서는 도형을 축소하고 하단 메뉴의 높이를 늘리되, 도형의 종횡비와 노드 좌표는 유지한다.

## 11. 금지 목록

- 홈 화면에 통계 대시보드 추가
- streak 숫자, 경쟁 순위, 좋아요, 팔로워 수 표시
- 도형을 설명하는 긴 튜토리얼 문구를 홈에 상시 표시
- 광고 배너나 유료 기능을 인증 동작 위에 배치
- 하루 5개 루틴을 한 화면의 복잡한 체크리스트로 고정 노출
- 완료되지 않은 점을 실제 완료 점처럼 표시
- 실제 사용자가 없을 때 합성 점이나 합성 사용자를 표시
- 도형 선택과 루틴 설정을 매 인증마다 요구

## 12. 구현 및 테스트 기준

- 35개 노드가 모든 지원 화면 비율에서 겹치지 않는다.
- 각 노드의 완료 이벤트가 올바른 요일과 루틴에 연결된다.
- 점 완료 순서와 연결선이 형상 메타데이터와 일치한다.
- 주간 도형 선택은 새 주에만 가능하고, 진행 중 기록을 잃지 않는다.
- 메뉴를 열고 닫아도 인증 상태와 replay timer가 변하지 않는다.
- reduce motion, 큰 글자, VoiceOver/TalkBack에서 콘텐츠가 겹치지 않는다.
- 오프라인 인증과 재시도에서도 같은 노드가 중복으로 채워지지 않는다.
