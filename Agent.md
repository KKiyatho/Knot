# Knot 구현 에이전트 가이드

이 문서는 매듭(Knot) 앱을 구현하거나 리뷰하는 에이전트가 따라야 할 작업 계약이다. 제품 요구사항의 기준은 [PRD.md](PRD.md), 화면 구현의 기준은 [UI.md](UI.md)다.

## 1. 제품 맥락

Knot은 글, 사진, 프로필, 좋아요 없이 매일 5개 루틴을 7일간 완료해 35개 노드의 형상을 완성하는 익명 루틴 앱이다. 핵심 경험은 다음 순서다.

`주간 도형 선택 -> ready -> 2초 hold -> 노드 채움과 선 연결 -> 익명 매칭 점 재생 -> 다음 루틴`

사용자에게 보이는 것은 주간 도형 틀, 루틴명, 점, 얇은 선, 짧은 햅틱과 소리뿐이다. 기능을 추가할 때 이 단순성이 실제 사용 부담을 낮추는지 먼저 판단한다.

## 2. 구현 우선순위

1. 인증 루프의 정확성: 2초 롱프레스, 조기 해제, 중복 방지
2. 매칭의 연속성: Tier 1 -> Tier 2 -> Tier 3 fallback
3. 익명성과 데이터 최소화
4. 오프라인/재시도 안정성
5. 감각적 렌더링과 접근성
6. Temporal Knot Engine의 지연·품질·비용 최적화
7. 관측성, 수익 모델, 운영 도구

애니메이션이나 사운드보다 서버 멱등성과 보안 규칙을 먼저 완성한다. 실제 동시 사용자가 없다는 이유로 운영 코드에 가짜 사용자를 생성하지 않는다.

기술적 차별화는 익명 시간 매칭, 서버 권위 시각, 후보 품질 제어, 기기별 replay 최적화의 결합에서 만든다. 개인 데이터 판매나 타깃 광고를 수익화 전략으로 사용하지 않는다.

## 3. 기본 기술 결정

- 클라이언트: Flutter/Dart
- 인증: Firebase Anonymous Auth
- 실시간 데이터: Firebase Realtime Database
- 서버 검증: Cloud Functions for Firebase 또는 프로젝트에서 채택한 동등한 trusted backend
- 웹 배포: Azure Static Web Apps
- 테스트: Dart/Flutter 단위 테스트, 위젯 테스트, 통합 테스트, Firebase Rules 테스트

저장소에 기술 선택이 추가되면 이 문서와 PRD의 권장 스택을 함께 갱신한다. 프레임워크를 새로 도입하거나 교체하기 전에 사용자에게 확인이 필요한 설계 변경인지 판단한다.

## 4. 아키텍처 경계

### 4.1 Flutter 계층

권장 경계:

- `presentation`: 화면, 제스처, 도형 렌더링, 애니메이션, 접근성
- `domain`: 루틴 상태, hold 판정, 주간 35개 노드, 형상 연결, 후보 replay 계획
- `data`: Firebase adapter, 익명 인증, 네트워크 재시도
- `services`: 햅틱, 오디오, 시간, 관측성 추상화

UI가 Firebase SDK를 직접 호출하지 않게 한다. 시간과 햅틱은 인터페이스로 감싸 테스트에서 가짜 구현을 주입할 수 있어야 한다.
형상 카탈로그는 버전 관리되는 순수 데이터로 두고, 화면은 `shapeId`와 노드 상태를 받아 렌더링한다. 노드 좌표나 연결 규칙을 위젯에 하드코딩하지 않는다.

### 4.2 서버 경계

클라이언트는 이벤트를 사실로 선언할 수 있지만, 서버는 다음을 검증한다.

- 인증된 익명 세션인지
- 허용된 루틴인지
- 서버 시간 기준 유효한 요청인지
- 같은 날짜/루틴의 중복 claim인지
- rate limit을 초과했는지
- 이벤트가 TTL 범위 안인지

클라이언트가 보낸 `occurredAt`, `tier`, `replayDelayMs`는 신뢰하지 않는다. `eventId`는 멱등성 키로만 사용하고 서버가 최종 시간을 부여한다.

## 5. 매칭 구현 규칙

### 후보 수집

- 현재 세션은 항상 제외한다.
- 같은 `eventId`는 deduplicate한다.
- Tier 우선순위는 시간순보다 높다.
- Tier 1은 같은 루틴의 서버 시간 기준 ±2초를 사용한다.
- Tier 2는 최근 1분, Tier 3은 최근 1시간이다.
- 이벤트는 1시간 TTL 후 읽기 대상에서 제외한다.
- 결과는 최대 8개로 제한하고, 정상 상황에서는 최소 1개를 목표로 한다.

### 재생 계획

서버는 후보의 출처와 서버 시각을 반환하고, 클라이언트는 이를 화면 재생 계획으로 변환한다.

- 사용자 자신의 점은 즉시 표시한다.
- Tier 1은 즉시에서 800ms 사이의 짧은 지연을 허용한다.
- Tier 2/3은 후보 사이에 1~2초 간격을 둔다.
- 전체 재생 시간은 기본 5초, 상한 10초다.
- 새 인증이 발생하면 이전 replay를 취소하고 새 replay를 시작한다.
- replay timer, stream subscription, animation controller는 화면 dispose 시 반드시 정리한다.

과거 이벤트를 현재 이벤트로 변조하거나 합성 사용자를 노출하는 구현은 금지한다. “항상 연결”은 fallback 후보와 차분한 빈 상태 처리로 달성한다.

매칭 replay는 주간 도형의 35개 노드와 다른 시각적 계층으로 처리한다. replay 초기화가 완료되어도 이미 인증된 주간 노드와 연결선은 사라지지 않는다.

## 6. 보안과 개인정보

- 원시 이메일, 전화번호, 위치, 연락처, 광고 식별자를 추가하지 않는다.
- UI와 클라이언트 로그에 다른 세션의 식별자를 표시하지 않는다.
- 타인의 데이터를 직접 읽는 Database path를 클라이언트에 열지 않는다.
- 이벤트 쓰기는 trusted backend 또는 검증 가능한 제한 경로를 사용한다.
- 같은 세션/루틴/날짜의 claim은 transaction 또는 서버 원자 연산으로 한 번만 허용한다.
- Rules 변경에는 허용 케이스와 거부 케이스 테스트를 함께 추가한다.
- 로그에는 개인 식별 가능 원문 대신 opaque id, 해시, 집계값을 사용한다.
- 새 개인정보 수집이 필요하면 구현 전에 PRD의 개인정보 범위와 보존 기간을 갱신한다.
- 주간 형상 기록은 `weekKey`, `shapeId`, `nodeId`, `eventId`만 저장하고, 화면용 좌표와 연결 목록은 형상 카탈로그에서 가져온다.

## 7. UI 및 상호작용 규칙

- 기본 화면은 따뜻한 아이보리/베이지 배경과 얇은 검정/다크그레이 선을 사용한다.
- 카드, 배지, 장식 아이콘, 소셜 지표를 기본 화면에 추가하지 않는다.
- 텍스트는 루틴명과 필요한 상태/접근성 라벨 중심으로 제한한다.
- 2초 hold는 프레임 시간에 의존하지 않고 monotonic clock 기반으로 계산한다.
- 손가락 이탈, 앱 백그라운드 진입, 포인터 취소를 모두 hold 취소로 처리한다.
- 햅틱/오디오가 실패해도 핵심 인증 상태는 성공할 수 있어야 한다.
- `MediaQuery.disableAnimations` 또는 플랫폼 reduce motion 설정을 존중한다.
- 색상, 진동, 소리만으로 상태를 구분하지 않는다.
- 작은 화면, 큰 글자 설정, VoiceOver/TalkBack에서 선과 텍스트가 겹치지 않게 한다.
- 기본 화면에는 주간 도형 틀, 현재 노드, 하단 루틴 메뉴 외의 카드를 추가하지 않는다. 상세 규칙은 [UI.md](UI.md)를 우선한다.

## 8. 오류 처리 원칙

사용자에게는 복구 가능한 다음 행동만 보여준다.

- 서버 지연: 재시도 중 상태와 중복 없는 자동 재시도
- 네트워크 단절: 오프라인 상태, 로컬 미확정 표시, 재연결 제출
- 중복 제출: 기존 매칭 결과 재사용
- 후보 0개: 내 매듭을 완료 상태로 유지한 뒤 조용히 초기화
- 오디오/햅틱 실패: 별도 오류 화면을 띄우지 않고 시각 피드백 유지

네트워크 재시도는 같은 `eventId`를 사용해야 한다. 무한 재시도, 빠른 polling, 사용자 입력을 막는 전역 오류 모달은 사용하지 않는다.

## 9. 작업 방식

### 변경 전
- 관련 요구사항과 소유 계층을 PRD에서 확인한다.
- 가장 가까운 테스트나 호출부를 먼저 읽는다.
- 작은 변경 단위와 이를 반증할 수 있는 가장 싼 검증을 정한다.
- 기존 사용자 변경 사항을 되돌리지 않는다.

### 변경 중
- 기존 프로젝트 스타일과 의존성을 따른다.
- 한 번에 한 동작 경계를 수정한다.
- 새 추상화는 테스트 가능성이나 실제 복잡도 감소가 있을 때만 추가한다.
- 공개 API와 저장 데이터 계약을 변경하면 문서와 마이그레이션을 함께 갱신한다.
- 코드는 자명한 경우 주석을 추가하지 않는다.

### 변경 후
- 첫 검증은 수정 범위에 가장 가까운 테스트 또는 타입/정적 검사로 한다.
- Firebase Rules, 멱등성, 타이머 dispose, 오프라인 재시도를 확인한다.
- 테스트가 없는 사용자 흐름을 발견하면 구현과 함께 최소한의 회귀 테스트를 추가한다.
- 포맷터와 린터를 실행하고, 실패 시 관련 변경만 고친다.
- 완료 기준을 충족하지 못한 상태에서 “완료”로 보고하지 않는다.

## 10. 권장 작업 순서

1. Flutter 앱 셸과 환경 설정을 만든다.
2. 익명 인증과 루틴 seed를 연결한다.
3. 순수 Dart로 hold state machine을 구현하고 경계값 테스트를 작성한다.
4. 형상 카탈로그와 35개 노드 연결 규칙을 만들고 도형 렌더링 테스트를 작성한다.
5. 로컬 fake repository로 주간 노드 채움과 replay를 먼저 연결한다.
6. Firebase adapter와 멱등 이벤트 제출을 연결한다.
7. Tier 1을 구현하고 동시성 통합 테스트를 만든다.
8. Tier 2/3 fallback, TTL, rate limit, Rules 테스트를 추가한다.
9. 햅틱/오디오 adapter와 접근성 상태를 연결한다.
10. 실제 기기 및 느린 네트워크에서 수동 검증한다.
11. 집계형 관측성과 배포 설정을 추가한다.

초기 MVP에서 머신러닝을 도입하지 않는다. 규칙 기반 Temporal Knot Engine과 실제 지연·재방문·매칭 품질 지표를 먼저 검증한 뒤, 개인정보를 늘리지 않는 범위에서 최적화 모델 도입 여부를 결정한다.

각 단계는 이전 단계의 테스트가 통과한 뒤 진행한다. 화면을 먼저 꾸미기 위해 백엔드 계약을 임시로 흐리지 않는다.

## 11. Definition of Done

작업은 다음을 모두 만족할 때 완료로 간주한다.

- 요구사항에 맞는 동작과 실패 경로가 구현되어 있다.
- 관련 단위/위젯/통합 테스트가 통과한다.
- 2초 hold 경계와 중복 제출이 검증되어 있다.
- Tier 1/2/3 매칭과 후보 중복 제거가 검증되어 있다.
- Firebase Rules와 TTL 정책이 테스트되어 있다.
- 오프라인 재연결 시 같은 이벤트가 중복 생성되지 않는다.
- reduce motion, 오디오 실패, 햅틱 미지원 상태가 동작한다.
- 개인정보가 로그나 UI에 새로 노출되지 않는다.
- 포맷, 린트, 빌드가 통과한다.
- PRD 또는 데이터 계약에 영향을 준 변경은 문서화되어 있다.

## 12. 보수적 기본값

미결정 사항이 구현을 막을 때는 다음을 기본값으로 사용한다.

- 하루 한 루틴당 성공 claim 1회
- MVP 기본 루틴은 기상, 물 마시기, 잠들기
- 후보 최대 8개, replay 상한 10초
- 이벤트 보존 1시간
- 서버 시간 권위
- 사용자 식별 정보 저장 없음
- 후보 0개일 때 합성 이벤트 없음
- 오디오와 햅틱은 선택 기능, 시각 피드백은 필수

이 기본값을 바꾸면 해당 결정과 이유를 PRD의 미결정 사항에서 확정 사항으로 옮긴다.
